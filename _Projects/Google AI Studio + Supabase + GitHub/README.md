# How to Integrate Supabase Backend Database into Your AI App | Google AI Studio + Supabase + GitHub

https://www.youtube.com/watch?v=86fCUwRaw1k

```prompt
Build a beautiful, stunning, modern, responsive SaaS-style Help Desk application named `TicketFlow` that allows small businesses to track customer issues through tickets, reply faster using AI-powered canned responses, and manage progress with clear statuses like Open, In Progress, and Resolved.

The UI should be colorful, clean, polished, and fully support dark mode. Dark mode should be elegant with smooth transitions when the user clicks Send Reply, the message should be added to the ticket conversation immediately, without reloading the page. After getting the status of the ticket while updating the ticket.

Add a basic Analytics Dashboard that shows key metrics: total tickets, open, in progress, and resolved. Include a header icon button that lets users switch between the Inbox and the Dashboard. The dashboard must use a clean, card-based layout that adapts properly in both light and dark modes.

In the Dashboard component, include a Recharts chart that visualizes the ticket status distribution (Open, In Progress, Resolved) over time.
In the Inbox component, add a filter dropdown that allows filtering tickets by status and a search bar.
Create smooth transitions between components from end to end, with a smooth, fast, effortless SaaS experience.
```

## Supabase setup

Create project

Project settings

## Integrate

In Google AI studio:

```prompt
I want to integrate this app with Supabase.
Below are the project details:
Project ID:
API Keys:
Project URL:
```

Open SQL Editor in Supabase, run the sql generated by AI
